package com.a.spaceescape2;

public class cordinates {
	
	/*
	//stage1
	public void addTarget() {
		face.setPosition(200,
		CAMERA_HEIGHT / 2);
		
		ball.setPosition(CAMERA_WIDTH - 200,
				CAMERA_HEIGHT / 2);
		}
	
	
		//stage2////////////////////////////////////
	 	x = 200 ;
		y = CAMERA_HEIGHT / 4;
		face.setPosition(x,y);
		
		x = CAMERA_WIDTH - 200 ;
		y = CAMERA_HEIGHT - 200;
		ball.setPosition(x,y);
		
		for(int i = 0; i < 7; i++) {
			
				switch(i){
				case 0:
					x = (CAMERA_WIDTH / 2) + face.getWidthScaled() ;
					y = CAMERA_HEIGHT / 4;
					break;
					
				case 1:
					x = CAMERA_WIDTH /2  - face.getWidthScaled() ;
					y = CAMERA_HEIGHT - 200;
					break;
				
				case 3:
					x = (CAMERA_WIDTH / 2) + face.getWidthScaled() ;
					y = (CAMERA_HEIGHT / 4) - face.getHeightScaled();
					break;
				
				case 5:
					x = (CAMERA_WIDTH / 2);
					y = (CAMERA_HEIGHT / 4) - face.getHeightScaled();
					break;
				
				case 4:
					x = CAMERA_WIDTH /2  - face.getWidthScaled() ;
					y = CAMERA_HEIGHT - 200 + face.getHeightScaled();
					break;
				
				case 6:
					x = CAMERA_WIDTH /2  ;
					y = CAMERA_HEIGHT - 200 + face.getHeightScaled();
					break;
				}
		///////////////////////////////////
		 
		 
	
		 
		
		
	*/

}
